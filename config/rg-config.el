;;; rg-config.el --- Minimal ripgrep setup -*- lexical-binding: t; -*-

;;; Code:

(use-package rg
  :straight t
  ;;:config
  ;; Enable default keybindings (C-c s prefix)
  ;;(rg-enable-default-bindings)
)

(provide 'rg-config)
;;; rg-config.el ends here
